const nombre = "fuck you";
// nombre = "fuck you 2"

console.log(nombre);


// la diferencia entre let y constant es que en let el valor puede cambiar en la linea del tiempo de tu script
//  si asi lo decides ya que el script de ejeceuta de arriba hacia abajo haciendo que el mas ultimo se 
// actualize, constant no cambia la tratar de cambiar nombre te dara error,como regla general las variable o columnas no se les cambia
// el nombre por eso siempre utilizar const , mas sin embargo por necesidad necesitas hacer esto cambiar a let
// nota= practica(uncomment) cambia el  const y let revisa to console en chrome y veraz el error tambien 