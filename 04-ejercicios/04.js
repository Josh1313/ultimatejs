/**
 * Crear un algoritmo que imprima numeros impares
 * utilzaremos un while, if para que se ejecute 
 * el codigp
 * 
 */
let i = 0;
while (i <= 10) {
    if (i % 2!== 0) {
       console.log('impar', i); 
    }
    i++;
}